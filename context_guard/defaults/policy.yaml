# Context Guard's single policy.
version: 2

mode: observe

files:
  max_full_read_bytes: 200000
  require_range_above_bytes: 50000
  deny:
    - "**/node_modules/**"
    - "**/vendor/**"
    - "**/dist/**"
    - "**/build/**"
    - "**/coverage/**"
    - "**/.git/**"
    - "**/*.min.js"
    - "**/*.map"
    - "**/package-lock.json"
    - "**/yarn.lock"
    - "**/pnpm-lock.yaml"
    - "**/Cargo.lock"

commands:
  maximum_expected_output_lines: 500
  require_bounds:
    - "docker logs"
    - "docker compose logs"
    - "kubectl logs"
    - "git log"

search:
  require_path_scope: true
  maximum_results: 100

fail_closed_rules: []

skills:
  rules: []
